# CLAUDE.md

此文件为 Claude Code 在此代码库中工作时提供指导。

## 项目概述

**@tsfullstack/backend** - TypeScript 全栈后端，基于 Fastify + Prisma + ZenStack + Effect 构建。

## 核心架构

- **服务器层** (`src/server/`) - Fastify HTTP 服务器
- **API 层** (`src/api/`) - 模块化业务逻辑
- **Context 层** (`src/Context/`) - Effect Context 依赖注入
- **RPC 系统** (`src/rpc/`) - 类型安全的远程调用
- **数据库层** (`src/db/`) - ZenStack 增强的 Prisma
- **OAuth** (`src/OAuth/`) - GitHub 认证
- **动态项目** (`src/projects/`) - 可动态加载的后端模块

## 开发命令

```bash
# 开发和构建
pnpm dev              # 开发模式
pnpm build:lib        # 构建前端库
pnpm build            # 构建后端
pnpm tsc:b            # 类型检查,修改代码后可自行按需运行此命令检查修改是否正确
```

## 开发规范

- **类型安全**: 严格模式，禁用 `any`，优先使用 Effect
- **代码质量**: 函数式编程，充分利用类型推导
- **构建要求**: API/schema 修改 api 下的代码，或者 projects 下的代码后必须执行 `pnpm build:lib`
- **数据安全** 除非我明确要求，否则不能执行任何可能删除数据库数据的命令，对于数据库迁移文件如果和数据库内的迁移记录不一致可以选择创建一个新的基线来应用修改（类似 pnpm zenstack migrate resolve --applied 20260228184653_init_baseline
）
- 使用 pnpm zenstack 来替代本来的 pnpm prisma 命令
- 当前项目默认端口是 5209

<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Mar 1, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #12950 | 8:40 PM | ✅ | Database migration executed for schema cleanup | ~261 |
| #12841 | 5:48 PM | 🔵 | Located backend configuration files for GLM API key setup | ~187 |
</claude-mem-context>