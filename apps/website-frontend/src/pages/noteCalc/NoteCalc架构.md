# NoteCalc

一个可编辑的富文本，
动态的进行计算
支持变量引用

## ui 美化
相聚靠近的表达式的结果应该尽量在一条竖线上

哎，我提出一个很简单并且很暴力的算法，也就是当下一行跟上一行的宽度相差不超过7个字的时候，那么就把下一行跟上一行作为同一个组。永远都只看下一行跟上一行，不要管这个组的平均值是什么东西，永远只看当前组的最后一行跟下一行。然后直到最后，按照最长的那一行作为基准，咱们生成的就要在最长的那一行的最右侧一条出现。