# 生产构建环境变量（vp build --mode production 时加载，优先级高于 .env）
# API 与前端同源部署（nginx 反代到后端 5209），baseServer 必须为空串
# 否则 .env 里的 VITE_API_BASE_URL=http://localhost:5209 会被打进生产产物，
# 导致线上页面把 API 请求发到访问者本机的 5209 端口（ERR_CONNECTION_REFUSED）
VITE_API_BASE_URL=
